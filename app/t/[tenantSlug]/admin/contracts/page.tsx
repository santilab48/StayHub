import AdminShell from '../../../../../components/AdminShell'
export default async function Page({params}:{params:Promise<{tenantSlug:string}>}){const {tenantSlug}=await params;return <AdminShell slug={tenantSlug} title="Paperless Contract"><div className="grid"><div className="card"><h2>1. สร้างสัญญา</h2><p className="muted">เลือกห้องและผู้เช่า → snapshot ข้อมูล/ค่าเช่า/เงินประกัน → version</p></div><div className="card"><h2>2. ผู้เช่าเซ็น</h2><p className="muted">ลายเซ็นเก็บ private storage + timestamp + version + audit</p></div><div className="card"><h2>3. อนุมัติ</h2><p className="muted">เจ้าของ/แอดมินเซ็น → สร้าง final PDF → archive ห้ามแก้ฉบับเซ็นแล้ว</p></div></div><section className="section card"><strong>สถานะ</strong><p className="muted">draft → sent → tenant_signed → active → expired/terminated</p></section></AdminShell>}
