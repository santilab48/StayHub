import TenantShell from '../../../../../../components/TenantShell'
export default async function Page({params}:{params:Promise<{tenantSlug:string}>}){const {tenantSlug}=await params;return <TenantShell slug={tenantSlug} title="สัญญาเช่า"><div className="card"><span className="pill">Paperless</span><h2>สัญญาปัจจุบัน</h2><p className="muted">ระบบจะโหลด contract_snapshot ของ lease ที่ active ของผู้ใช้ LINE คนนี้เท่านั้น</p><div className="flow"><span>อ่านสัญญา</span><span>ยืนยันข้อมูล</span><span>เซ็นบนจอ</span><span>บันทึกเวลา/version</span><span>เจ้าของอนุมัติ</span><span>PDF final</span></div></div><section className="section card"><h2>หลักฐานการเซ็น</h2><p className="muted">signature path, signer, role, signed_at, user agent, contract version และ audit log แยกจากตัวสัญญา เพื่อห้ามการแก้ย้อนหลังแบบเงียบ ๆ</p><button className="btn">อ่านและเซ็นสัญญา</button></section></TenantShell>}
