import TenantShell from '../../../../../../components/TenantShell'
import BillingNav from '../../../../../../components/BillingNav'
export default async function Page({params}:{params:Promise<{tenantSlug:string}>}){const {tenantSlug}=await params;return <TenantShell slug={tenantSlug} title="แจ้งชำระ"><BillingNav slug={tenantSlug}/><section className="card"><h2>อัปโหลดสลิป</h2><p className="muted">เลือกบิลที่ต้องการชำระ ตรวจยอด แล้วแนบหลักฐานการโอน เจ้าของจะเป็นผู้ตรวจและอนุมัติ</p><div className="formGrid"><label>บิล<select><option>เลือกบิล</option></select></label><label>ยอดที่ชำระ<input placeholder="0.00" inputMode="decimal"/></label><label className="span2">แนบสลิป<input type="file" accept="image/*,application/pdf"/></label><label className="span2">หมายเหตุ<textarea rows={3} placeholder="เช่น โอนจากบัญชี..."/></label></div><button className="btn">ส่งหลักฐานการชำระ</button></section><section className="section card noticeBox"><strong>หลังส่งแล้ว</strong><p className="muted">สถานะจะเป็น “รอตรวจ” และผู้เช่าแก้ยอดบิลเองไม่ได้ เจ้าของต้องกดอนุมัติหรือปฏิเสธจากหลังบ้านการเงิน เมื่ออนุมัติแล้วระบบออกใบเสร็จ</p></section></TenantShell>}
