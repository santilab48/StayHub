import './globals.css'
export const metadata = { title: 'StayHub', description: 'LINE OA dormitory web app core' }
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="th"><body>{children}</body></html>}
